version: '3'
services:
  app:
    image: "maven-automated-pipeline:$BUILD_TAG"
    build:
      context: .
      dockerfile: Dockerfile-Java
    ports:
      - "8080:8080"
    networks:
      - net
networks:
  net: