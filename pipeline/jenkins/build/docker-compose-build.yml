version: '3'
services:
  app:
    image: "maven-automated-pipeline:$BUILD_TAG"
    build:
      context: .
      dockerfile: Dockerfile-Java
    ports:
      - "8081:8081"
    networks:
      - net
networks:
  net: